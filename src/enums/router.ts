export enum ViewIds {
  Main = 'Main',
  Shop = 'Shop'
}

export enum PanelIds {
  // panels for Main view
  Main = 'Main',
  Panel2 = 'Panel2',

  // panels for Shop view
  Shop = 'Shop',
}

export enum ModalIds {
  TestModal = 'TestModal',
}
